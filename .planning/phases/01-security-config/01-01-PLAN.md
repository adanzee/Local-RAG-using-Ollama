---
phase: 01-security-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/config.py, .env.example, .gitignore]
autonomous: true
requirements: [REQ-SEC-01]
user_setup: []

must_haves:
  truths:
    - Configuration values loaded from environment variables
    - Sensitive data not hardcoded in source code
    - Default values provided for optional configs
  artifacts:
    - app/config.py: Module for loading and validating configuration
    - .env.example: Template file with all required environment variables
    - .gitignore: Updated to ignore .env files
  key_links:
    - app/config.py imported in app/database.py
    - app/config.py imported in app/model.py
    - Environment variables loaded at application startup
---

<objective>
Implement secure configuration management system to eliminate hardcoded secrets and provide centralized config handling.

Purpose: Prevent security vulnerabilities from exposed credentials and enable environment-specific configurations.
Output: Config module and environment template.
</objective>

<execution_context>
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/workflows/execute-plan.md
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/database.py
@app/model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configuration module</name>
  <files>app/config.py</files>
  <action>Create app/config.py with functions to load environment variables using python-dotenv, provide defaults, and validate required configs. Include database, model, and Ollama settings.</action>
  <verify>python -c "from app.config import get_db_config, get_model_config; print('Config loads successfully')"</verify>
  <done>config.py exists and can be imported without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create environment template</name>
  <files>.env.example</files>
  <action>Create .env.example with all required environment variables documented, including database connection details, model names, and Ollama URL.</action>
  <verify>grep -c "DB_NAME\|OLLAMA_URL" .env.example > 0</verify>
  <done>.env.example contains all config variables with comments</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore for security</name>
  <files>.gitignore</files>
  <action>Add .env to .gitignore to prevent committing sensitive environment files.</action>
  <verify>grep -q "^\.env$" .gitignore</verify>
  <done>.env is in .gitignore</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → File paths | Untrusted file paths from user input |
| Environment → Config | Environment variables may be compromised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Hardcoded secrets | mitigate | Move all secrets to environment variables |
| T-01-02 | Spoofing | Config validation | mitigate | Validate config values on load |
</threat_model>

<verification>
Run config import test and verify no hardcoded values in code.
</verification>

<success_criteria>
Configuration system prevents hardcoded secrets and supports environment-specific setups.
</success_criteria>

<output>
After completion, create .planning/phases/01-security-config/01-01-SUMMARY.md
</output>