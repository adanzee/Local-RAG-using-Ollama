---
phase: 01-security-config
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [app/database.py, app/llm_query.py, app/main.py, app/check_db.py]
autonomous: true
requirements: [REQ-SEC-01]
user_setup: []

must_haves:
  truths:
    - No hardcoded database credentials in source code
    - User inputs validated for path traversal and injection
    - Database connections use config values
  artifacts:
    - app/database.py: Uses config for connection parameters
    - app/llm_query.py: Uses config for DB_CONFIG
    - app/main.py: Uses config and validates file paths
    - app/check_db.py: Uses config for test connection
  key_links:
    - config.get_db_config() called in DatabaseHandler.__init__
    - File paths sanitized before processing
---

<objective>
Remove all hardcoded secrets from code and implement input validation to prevent security vulnerabilities.

Purpose: Eliminate credential exposure and protect against injection attacks.
Output: Secure code with validated inputs.
</objective>

<execution_context>
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/workflows/execute-plan.md
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-config/01-01-SUMMARY.md
@app/database.py
@app/llm_query.py
@app/main.py
@app/check_db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update database.py to use config</name>
  <files>app/database.py</files>
  <action>Replace hardcoded connection parameters in DatabaseHandler.__init__ with calls to config.get_db_config(). Import config module.</action>
  <verify>grep -v "from config" app/database.py | grep -c "dbname\|password\|host" == 0</verify>
  <done>DatabaseHandler uses config for all connection params</done>
</task>

<task type="auto">
  <name>Task 2: Update llm_query.py to use config</name>
  <files>app/llm_query.py</files>
  <action>Remove DB_CONFIG dict and replace with config.get_db_config() call. Update DatabaseHandler instantiation.</action>
  <verify>grep -c "DB_CONFIG" app/llm_query.py == 0</verify>
  <done>llm_query.py uses config for database setup</done>
</task>

<task type="auto">
  <name>Task 3: Secure main.py and add validation</name>
  <files>app/main.py</files>
  <action>Replace hardcoded DB params with config. Add file path validation using os.path.abspath and check for path traversal attempts.</action>
  <verify>python -c "import app.main; print('No syntax errors')"</verify>
  <done>main.py uses config and validates file paths</done>
</task>

<task type="auto">
  <name>Task 4: Update check_db.py</name>
  <files>app/check_db.py</files>
  <action>Replace hardcoded values with config.get_db_config() call.</action>
  <verify>grep -c "rag_db\|@123\|localhost\|5432\|postgres" app/check_db.py == 0</verify>
  <done>check_db.py uses config for test connection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → File system | File paths could contain traversal attacks |
| Code → Database | Connection params must be protected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Hardcoded DB creds | mitigate | Use config module for all credentials |
| T-01-04 | Tampering | File path input | mitigate | Validate and sanitize file paths |
</threat_model>

<verification>
Run all scripts and verify no hardcoded values, configs load from env.
</verification>

<success_criteria>
All hardcoded secrets removed, inputs validated, system uses secure configuration.
</success_criteria>

<output>
After completion, create .planning/phases/01-security-config/01-02-SUMMARY.md
</output>