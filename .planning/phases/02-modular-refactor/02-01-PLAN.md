---
phase: 02-modular-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/services/__init__.py, app/services/ingestion_service.py, app/services/query_service.py, app/services/model_service.py]
autonomous: true
requirements: [REQ-MOD-01]
user_setup: []

must_haves:
  truths:
    - Core functionality separated into independent service modules
    - Services have clear interfaces and responsibilities
    - No direct database/model calls in main scripts
  artifacts:
    - app/services/ingestion_service.py: Handles document processing and storage
    - app/services/query_service.py: Manages chat and retrieval
    - app/services/model_service.py: Wraps model operations
    - app/services/__init__.py: Exports service classes
  key_links:
    - ingestion_service used in main.py
    - query_service used in llm_query.py
    - Services depend on config and database/model handlers
---

<objective>
Extract monolithic code into modular service classes with clear responsibilities.

Purpose: Improve maintainability, testability, and separation of concerns.
Output: Service modules for ingestion, querying, and modeling.
</objective>

<execution_context>
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/workflows/execute-plan.md
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-config/01-02-SUMMARY.md
@app/main.py
@app/llm_query.py
@app/engine.py
@app/model.py
@app/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create services directory and init</name>
  <files>app/services/__init__.py</files>
  <action>Create app/services/ directory and __init__.py file to make it a package.</action>
  <verify>test -d app/services && test -f app/services/__init__.py</verify>
  <done>Services package exists</done>
</task>

<task type="auto">
  <name>Task 2: Extract ingestion service</name>
  <files>app/services/ingestion_service.py</files>
  <action>Create IngestionService class that encapsulates document loading, chunking, embedding, and storage. Move logic from main.py and engine.py.</action>
  <verify>python -c "from app.services.ingestion_service import IngestionService; print('Service imports')"</verify>
  <done>IngestionService class created with process_document method</done>
</task>

<task type="auto">
  <name>Task 3: Extract query service</name>
  <files>app/services/query_service.py</files>
  <action>Create QueryService class for chat functionality, retrieval, and history management. Move logic from llm_query.py.</action>
  <verify>python -c "from app.services.query_service import QueryService; print('Service imports')"</verify>
  <done>QueryService class created with ask_question and manage_history methods</done>
</task>

<task type="auto">
  <name>Task 4: Extract model service</name>
  <files>app/services/model_service.py</files>
  <action>Create ModelService class wrapping ModelHandler for embedding and LLM operations.</action>
  <verify>python -c "from app.services.model_service import ModelService; print('Service imports')"</verify>
  <done>ModelService class created with embed and generate methods</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service interfaces | Clear contracts between services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Service interfaces | mitigate | Define clear service contracts |
</threat_model>

<verification>
All service classes can be imported and instantiated.
</verification>

<success_criteria>
Core functionality extracted into modular services with clear interfaces.
</success_criteria>

<output>
After completion, create .planning/phases/02-modular-refactor/02-01-SUMMARY.md
</output>