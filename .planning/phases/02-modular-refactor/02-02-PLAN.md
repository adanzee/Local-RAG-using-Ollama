---
phase: 02-modular-refactor
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [app/services/container.py, app/main.py, app/llm_query.py]
autonomous: true
requirements: [REQ-MOD-01]
user_setup: []

must_haves:
  truths:
    - Services instantiated through dependency injection container
    - Main scripts use container to get service instances
    - Dependencies wired automatically
  artifacts:
    - app/services/container.py: Factory for creating service instances with dependencies
    - app/main.py: Uses container to get IngestionService
    - app/llm_query.py: Uses container to get QueryService
  key_links:
    - container.get_ingestion_service() called in main.py
    - container.get_query_service() called in llm_query.py
---

<objective>
Implement dependency injection to wire services together and remove tight coupling.

Purpose: Enable testability and flexibility in service composition.
Output: DI container and updated main scripts.
</objective>

<execution_context>
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/workflows/execute-plan.md
@F:/Local-RAG-using-Ollama/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-modular-refactor/02-01-SUMMARY.md
@app/services/ingestion_service.py
@app/services/query_service.py
@app/main.py
@app/llm_query.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dependency injection container</name>
  <files>app/services/container.py</files>
  <action>Create container.py with functions to instantiate services with their dependencies (config, db, model handlers).</action>
  <verify>python -c "from app.services.container import get_ingestion_service; print('Container imports')"</verify>
  <done>Container provides factory methods for all services</done>
</task>

<task type="auto">
  <name>Task 2: Update main.py to use services</name>
  <files>app/main.py</files>
  <action>Replace direct handler instantiation with container.get_ingestion_service(). Update the ingestion loop to use service methods.</action>
  <verify>python -c "import app.main; print('main.py runs without errors')"</verify>
  <done>main.py uses IngestionService from container</done>
</task>

<task type="auto">
  <name>Task 3: Update llm_query.py to use services</name>
  <files>app/llm_query.py</files>
  <action>Replace direct instantiations with container.get_query_service(). Update the query loop to use service methods.</action>
  <verify>python -c "import app.llm_query; print('llm_query.py runs without errors')"</verify>
  <done>llm_query.py uses QueryService from container</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container → Services | Dependencies injected securely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | DI wiring | mitigate | Validate service dependencies |
</threat_model>

<verification>
Main scripts run and use services without direct handler instantiation.
</verification>

<success_criteria>
Services wired through dependency injection, main scripts decoupled from implementation details.
</success_criteria>

<output>
After completion, create .planning/phases/02-modular-refactor/02-02-SUMMARY.md
</output>